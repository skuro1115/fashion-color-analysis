@AGENTS.md

Claude Code 用の追記:

- 利用者は日本語で話す非エンジニアが多い。返答は日本語で、専門用語は短く補足する。
- 依頼は4つのモード (`/request` 要望を整える、`/guide` 使い方を理解する、`/run-analysis` 実行する、`/update` 最新版にする) のどれかで受ける。
  利用者がスキル名を知らなくても、内容から判断して該当スキルを使う。
- 大きな変更 (新しい画面・依存追加・出力形式の変更) は、実装前に `/request` で要望書を作って確認を取る。
- 変更後は `uv run pytest -q` と samples での実行結果を報告する。
- 起動時フックが「新しい版があります」と知らせた場合は、最初の返答で `/update` を一言提案する (勝手に更新しない)。
